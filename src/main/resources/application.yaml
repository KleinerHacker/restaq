spring:
  application:
    name: restqa

# Global queue technology selector (amqp = RabbitMQ default, jms = Artemis).
# Broker connectivity is configured via the Spring Boot standard
# spring.rabbitmq.* / spring.artemis.* properties (left empty here on purpose).
restqa:
  type: amqp
  # sender:
  #   <name>:
  #     rest:
  #       path: /...
  #     queue:
  #       name: ...
  # receiver:
  #   <name>:
  #     rest:
  #       url: https://...
  #     queue:
  #       name: ...
